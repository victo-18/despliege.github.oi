//import logo from "./logo.svg";
import "./App.css";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>
          Sin embargo, cuando ingreso ese comando, desde Windows PowerShell, Git
          Bash y el símbolo del sistema, solo obtengo varias variaciones de
          "comando no encontrado". En respuesta, desinstalé gh-pages de las
          dependencias de desarrollo y lo reinstalé como una dependencia normal.
          Ningún cambio. En este punto, he agotado todas las soluciones que se
          me ocurren y Google solo ha identificado a las personas que no
          incluyeron el comando de implementación en package.json.
        </p>
      </header>
    </div>
  );
}

export default App;
